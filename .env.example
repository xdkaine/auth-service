
# Optional live Kubernetes TokenReview integration. Never commit the actual secret.
# Configure both only when replacing the matching offline JWT authenticator.
KUBERNETES_OIDC_CLIENT_ID=
KUBERNETES_WEBHOOK_SECRET=
